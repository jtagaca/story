//jshint esversion:6
//npm init to install the npm package, be on the right foler that you want
const t = require("superheroes"); //require in order to use an API you have to install it first 
//command on the folder is "npm install name_of_the_package" 

var my = t.random();
console.log(my);